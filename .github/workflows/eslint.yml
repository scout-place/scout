name: ESLint

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
       run:
         working-directory: ./backend/

    steps:
    - uses: actions/checkout@v2
    - name: Install modules
      run: npx
    - name: Run ESLint
      run: eslint .